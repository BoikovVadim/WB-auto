.DS_Store
node_modules
dist
coverage
.env
.env.local
.env.*.local
.env.deploy.local
Frontend/dist
backend/dist
backend/data
*.log
Frontend/build

# TS incremental build artifact (генерируется сборкой)
*.tsbuildinfo

# Сгенерированные из vite.config.ts артефакты (source of truth — .ts)
Frontend/vite.config.js
Frontend/vite.config.d.ts

# Артефакт поставки в Oqqi — не хранить в репозитории
oqqi-sales.bundle
oqqi-db-core.dump

# Локальные настройки Claude (автономия оператора) — не для поставки
.claude/settings.local.json
.claude/scheduled_tasks.lock
